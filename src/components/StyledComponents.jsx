import styled from "styled-components";
import {
  StyledContainer,
  StyledTitle,
  StyledButton
} from "./style/StyledContainer";

export const StyledComponents = () => {
  return (
    <StyledContainer>
      <StyledTitle> -- Styled Components -- </StyledTitle>
      <StyledButton>FIGHT</StyledButton>
    </StyledContainer>
  );
};
